# Canopy MCP Protocol

The canonical Phase 5 MCP protocol documentation is maintained at:

`docs/mcp/PROTOCOL.md`

This service implements the documented stdio MCP server using the official
`@modelcontextprotocol/sdk` and forwards all tool calls to the Canopy REST API.
